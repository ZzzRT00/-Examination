<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xhj.examination.mapper.ExamRecordMapper">

    <insert id="insert">
        INSERT INTO exam_record(student_id, paper_id, score, exam_time)
        VALUES (#{studentId}, #{paperId}, #{score}, #{examTime})
    </insert>

    <select id="selectByStudentId" resultType="com.xhj.examination.entity.ExamRecord">
        SELECT id,student_id,paper_id,score,exam_time
        FROM exam_record
        WHERE student_id = #{studentId}
    </select>

    <select id="selectById" resultType="com.xhj.examination.entity.ExamRecord">
        SELECT id,student_id,paper_id,score,exam_time
        FROM exam_record
        WHERE id = #{id}
    </select>

</mapper>