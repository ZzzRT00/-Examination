spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mybatis_db?useUnicode=true&characterEncoding=utf-8&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  aop:
    proxy-target-class: true
  profiles:
    active: test
mybatis:
  type-aliases-package: com.xhj.examinatioin
  mapper-locations: classpath:mapper/*.xml

